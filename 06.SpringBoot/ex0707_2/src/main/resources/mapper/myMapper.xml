<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.member.mapper.MyMapper">
	<select id="selectMember" resultType="com.member.model.Member">
		SELECT * FROM member
		ORDER BY id
	</select>
</mapper>